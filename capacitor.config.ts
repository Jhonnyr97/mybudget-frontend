import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nilthon.com.mybudget.app',
  appName: 'MyBudget',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
